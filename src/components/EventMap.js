/* eslint-disable no-console */
import React from "react";
import Image from "./Image";

const EventMap = () => (
  <>
    <h1 className="section-header">Event Map</h1>
    <Image fileName="event-map.png" />
  </>
);

export default EventMap;
